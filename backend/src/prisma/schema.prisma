generator client {
    provider   = "prisma-client"
    output     = "./generated"
    engineType = "client"
}

datasource db {
    provider = "postgresql"
}

// Example model, DELETE AFTERWARDS
model Category {
    id   Int    @id @default(autoincrement())
    slug String @unique
    name String
}
