import { detectConflict } from "./detectConflict";
import { getNextName } from "./getNextName";

export class ConflictsUtils {
  static readonly detectConflict = detectConflict;
  static readonly getNextName = getNextName;
}
