import persistNode from "./persistNode";
import resolveNodeIdentity from "./resolveNodeIdentity";

export class ProcessUtils {
  static readonly persistNode = persistNode;
  static readonly resolveNodeIdentity = resolveNodeIdentity;
}
